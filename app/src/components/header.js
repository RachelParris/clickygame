import React from 'react';

// Functional, Presentational, Stateless, Dumb Component
const Header = () => (
  <div className="jumbotron">
    <h1>Clicky Game!</h1>
    <p>Click on an image to earn points, but don't click on any more than once!</p>
  </div>
);

export default Header;
